---
sidebar_position: 1
---

import MirrorSources from '@site/src/components/MirrorSources';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import PlatformIcon from '@site/src/components/PlatformIcon';

# CentOS Vault 镜像使用帮助

## 简介

CentOS Vault 是储存 CentOS 过时版本的仓库。CentOS 各版本在停止维护后，其软件包会被归档到 CentOS Vault，方便用户继续获取旧版本的软件包。

## 镜像信息

- **架构**：全部
- **版本**：全部
- **更新策略**：由于镜像占用空间大且访问量少，采用缓存方式，所有文件缓存30天

## 使用说明

首先备份 `/etc/yum.repos.d/CentOS-Base.repo`

```bash
mv /etc/yum.repos.d/CentOS-Base.repo /etc/yum.repos.d/CentOS-Base.repo.backup
```

根据对应的CentOS版本，编辑 `/etc/yum.repos.d/CentOS-Base.repo` 文件, 修改为对应内容。（详见后面配置参考）

最后运行以下命令生成缓存

```bash
yum clean all
yum makecache
```

## 配置参考

根据你的 CentOS 版本选择对应的配置：

<Tabs groupId="centos-version">
  <TabItem value="centos6-7" label={<PlatformIcon name="centos" label="CentOS 6/7" />}>

<MirrorSources
  type="yum"
  host="mirrors.gdut.edu.cn"
  path="/centos-vault"
  versions={[
    { label: 'CentOS 6', codename: '6' },
    { label: 'CentOS 7', codename: '7' },
  ]}
  repos={[
    { id: 'base', name: 'CentOS-$releasever - Base', pathTemplate: '$releasever/os/$basearch/', gpgKey: 'file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-{codename}' },
    { id: 'updates', name: 'CentOS-$releasever - Updates', pathTemplate: '$releasever/updates/$basearch/', gpgKey: 'file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-{codename}' },
    { id: 'extras', name: 'CentOS-$releasever - Extras', pathTemplate: '$releasever/extras/$basearch/', gpgKey: 'file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-{codename}' },
    { id: 'centosplus', name: 'CentOS-$releasever - Plus', pathTemplate: '$releasever/centosplus/$basearch/', enabled: false, gpgKey: 'file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-{codename}' },
  ]}
  filePath="/etc/yum.repos.d/CentOS-Base.repo"
  options={{ https: false, source: false, proposed: false, security: false, sudo: true }}
  quickConfigType="yum"
/>

  </TabItem>
  <TabItem value="centos8" label={<PlatformIcon name="centos" label="CentOS 8" />}>

:::note[版本说明]

CentOS 8 已于 2021 年 12 月 31 日结束维护，最终小版本为 **8.5.2111**。下方配置中的 `$releasever` 需替换为该完整版本号。

:::

<MirrorSources
  type="yum"
  host="mirrors.gdut.edu.cn"
  path="/centos-vault"
  versions={[
    { label: 'CentOS 8 (8.5.2111)', codename: '8.5.2111' },
  ]}
  repos={[
    { id: 'BaseOS', name: 'CentOS-$releasever - Base', pathTemplate: '$releasever/BaseOS/$basearch/os/', gpgKey: 'file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial' },
    { id: 'AppStream', name: 'CentOS-$releasever - AppStream', pathTemplate: '$releasever/AppStream/$basearch/os/', gpgKey: 'file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial' },
    { id: 'PowerTools', name: 'CentOS-$releasever - PowerTools', pathTemplate: '$releasever/PowerTools/$basearch/os/', enabled: false, gpgKey: 'file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial' },
    { id: 'extras', name: 'CentOS-$releasever - Extras', pathTemplate: '$releasever/extras/$basearch/os/', gpgKey: 'file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial' },
    { id: 'centosplus', name: 'CentOS-$releasever - Plus', pathTemplate: '$releasever/centosplus/$basearch/os/', enabled: false, gpgKey: 'file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial' },
  ]}
  filePath="/etc/yum.repos.d/CentOS-Base.repo"
  options={{ https: false, source: false, proposed: false, security: false, sudo: true }}
  quickConfigType="yum"
/>

  </TabItem>
</Tabs>
